// Zone JS is required by default for Angular itself.
import 'zone.js';  // Included with Angular CLI.

/**
 * This file can be used to add global scrips, or to change the behaviour of zone.js
 */

// Prevent zone from patching requestAnimationFrame
// (window as any).__Zone_disable_requestAnimationFrame = true;

// Prevent zone from patching onProperty such as onclick
// (window as any).__Zone_disable_on_property = true;

// Disable patch customElements (for Angular Elements)
// (window as any).__Zone_disable_customElements = true;
